arg_max(x) = y[#y-1,1]
    
    ## FIXME: Variable x in type is not substituted:
    ## where y :: [#x,2]int = 
    
    where {
        y :: [6,2]int;
        y = [
            0 -> [x[0]; 0];
            i -> if x[i] > y[i-1,0]
                then [x[i]; i]
                else y[i-1]
        ];
    };

main = arg_max([3;0;5;9;4;2]);

##? int
##? 3
