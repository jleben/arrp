module array;

slice(pos,len,a) = [len:i -> a[pos + i]];

scan(f,a) = [
    0 -> a[0];
    i -> f(this[i-1], a[i]);
];

fold(f,a) =
    let s = scan(f,a) in
        s[#a-1];

iterate(f,init) = [
    0 -> init;
    i -> f(this[i-1]);
];

map(f,a) = [i -> f(a[i])]
